# ============================================
# AI ART ARENA - Environment Variables
# ============================================
# Copy this file to .env.local and fill in your actual values

# ============================================
# PUBLIC VARIABLES (exposed to browser)
# ============================================

# Application URL
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Supabase Configuration (Public)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# ============================================
# SERVER-ONLY VARIABLES (never exposed)
# ============================================

# Supabase Service Role Key (for server-side operations)
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Vercel Cron Secret (for securing cron endpoints)
# Generate a secure random string: openssl rand -base64 32
CRON_SECRET=your_secure_random_cron_secret

# IP Salt for vote hashing (optional but recommended)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
IP_SALT=your_random_salt_for_ip_hashing

# ============================================
# OPTIONAL VARIABLES
# ============================================

# Analytics
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Sentry Error Tracking
# SENTRY_DSN=your_sentry_dsn
# SENTRY_AUTH_TOKEN=your_sentry_auth_token

# Node Environment
# NODE_ENV=development

# ============================================
# SETUP INSTRUCTIONS
# ============================================
#
# 1. Supabase Setup:
#    - Go to https://supabase.com/dashboard
#    - Create a new project
#    - Copy the API URL and anon key from Settings > API
#    - Copy the service role key (use with caution!)
#
# 2. Generate Secrets:
#    Cron Secret: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
#    IP Salt: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# 3. Vercel Deployment:
#    - Add all environment variables in Vercel Dashboard
#    - Ensure CRON_SECRET matches between .env and Vercel
#    - Set NEXT_PUBLIC_SITE_URL to your production domain
#
# 4. Local Development:
#    - Copy this file to .env.local
#    - Fill in all required values
#    - Never commit .env.local to version control
#
